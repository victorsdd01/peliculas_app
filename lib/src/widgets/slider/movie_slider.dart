

import 'package:flutter/material.dart';

class MovieSliderPage extends StatelessWidget {
   
  const MovieSliderPage({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('MovieSliderPage'),
      ),
    );
  }
}